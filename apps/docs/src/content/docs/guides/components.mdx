---
title: Components
description: Live, interactive examples of Vespera components — with the code to add them.
---

import Preview from '../../../components/Preview.astro';
import {
  ButtonExample,
  BadgeExample,
  AlertExample,
  SelectExample,
  ToggleExample,
  TabsExample,
  OverlayExample,
  DataExample,
  DatePickerExample,
} from '../../../components/examples';
import {
  ChartsExample,
  FeedbackExample,
  StructureExample,
  CommandExample,
  SheetExample,
  DateRangeExample,
  NavExample,
  MessagingExample,
  MoreFormsExample,
  AvatarExample,
} from '../../../components/examples-more';

Live examples you can interact with, each with the code to copy. Everything renders inside a
themed `.vsp-root` — see [Installation](/vespera-ui/guides/installation/) to set up, and the
[API reference](/vespera-ui/reference/react/) for the full props of all 65 components.

## Buttons

<Preview
  code={`import { Button, Tooltip } from '@vespera-ui/react';
import { Icon } from '@vespera-ui/icons';

<Button variant="primary" leadingIcon={<Icon.check />}>Save</Button>
<Button variant="ghost">Cancel</Button>
<Button variant="outline">Outline</Button>
<Button variant="danger">Delete</Button>
<Button variant="primary" loading>Saving…</Button>
<Tooltip label="Notifications">
  <Button variant="ghost" leadingIcon={<Icon.bell />}>Hover me</Button>
</Tooltip>`}
>
  <ButtonExample client:visible />
</Preview>

## Badges & tags

<Preview
  code={`import { Badge, Tag } from '@vespera-ui/react';

<Badge tone="pos" dot>Active</Badge>
<Badge tone="neg" dot>Failed</Badge>
<Badge tone="warn" dot>Pending</Badge>
<Badge tone="info" dot>Beta</Badge>
<Badge tone="muted">Draft</Badge>
<Tag onRemove={() => {}}>Removable</Tag>`}
>
  <BadgeExample client:visible />
</Preview>

## Alerts

<Preview
  code={`import { Alert } from '@vespera-ui/react';

<Alert tone="info" title="Heads up">Your trial ends in 3 days.</Alert>
<Alert tone="pos" title="Saved">Your changes are live.</Alert>
<Alert tone="warn" title="Usage high">You're near your API limit.</Alert>`}
>
  <AlertExample client:visible />
</Preview>

## Selects

<Preview
  code={`import { Select, Combobox, MultiSelect, Field } from '@vespera-ui/react';

const fruits = ['Apple', 'Banana', 'Cherry', /* … */];

<Field label="Select"><Select options={fruits} value={a} onChange={setA} /></Field>
<Field label="Combobox"><Combobox options={fruits} value={b} onChange={setB} clearable /></Field>
<Field label="Multi-select"><MultiSelect options={fruits} value={c} onChange={setC} max={4} /></Field>`}
>
  <SelectExample client:visible />
</Preview>

## Toggles

<Preview
  code={`import { Checkbox, Switch, Slider } from '@vespera-ui/react';

<Checkbox checked={check} onChange={setCheck} label="Email notifications" sub="Activity updates" />
<Switch checked={on} onChange={setOn} aria-label="Two-factor" />
<Slider value={vol} onChange={setVol} />`}
>
  <ToggleExample client:visible />
</Preview>

## Tabs

<Preview
  code={`import { Tabs } from '@vespera-ui/react';

<Tabs
  tabs={[
    { value: 'overview', label: 'Overview' },
    { value: 'activity', label: 'Activity', count: 3 },
    { value: 'settings', label: 'Settings' },
  ]}
  value={tab}
  onChange={setTab}
/>`}
>
  <TabsExample client:visible />
</Preview>

## Overlays

<Preview
  code={`import { Button, Dialog, DropdownMenu, ToastHost, toast } from '@vespera-ui/react';
import { Icon } from '@vespera-ui/icons';

<Button variant="primary" onClick={() => setOpen(true)}>Open dialog</Button>
<DropdownMenu trigger={<Button trailingIcon={<Icon.chevDown />}>Menu</Button>} items={[
  { label: 'Profile', icon: <Icon.user /> },
  { sep: true },
  { label: 'Log out', icon: <Icon.logout />, danger: true },
]} />
<Button onClick={() => toast({ tone: 'pos', title: 'Saved' })}>Fire a toast</Button>
<Dialog open={open} onClose={() => setOpen(false)} tone="neg" title="Delete workspace?" /* … */ />
<ToastHost />`}
>
  <OverlayExample client:visible />
</Preview>

## Data display

<Preview
  code={`import { Stat, Timeline } from '@vespera-ui/react';
import { Icon } from '@vespera-ui/icons';

<Stat icon={<Icon.dollar />} label="Revenue" value="$48,200" delta="12%" deltaDir="up" />
<Timeline items={[
  { title: 'Workspace created', time: '2d ago', tone: 'pos', icon: <Icon.check /> },
  { title: 'Member invited', time: '1d ago', tone: 'info', icon: <Icon.user /> },
]} />`}
>
  <DataExample client:visible />
</Preview>

## Date picker

<Preview
  code={`import { DatePicker, Field } from '@vespera-ui/react';

<Field label="Date"><DatePicker value={d} onChange={setD} /></Field>`}
>
  <DatePickerExample client:visible />
</Preview>

## Charts

<Preview
  code={`import { AreaChart, BarChart, Donut, Sparkline } from '@vespera-ui/react';

<AreaChart series={[revenue]} labels={months} />
<BarChart data={[40, 65, 52, 78, 61, 90, 72]} labels={['M','T','W','T','F','S','S']} />
<Donut data={[
  { label: 'Organic', value: 48, color: 'var(--accent)' },
  { label: 'Referral', value: 30, color: 'var(--accent-2)' },
  { label: 'Direct', value: 22, color: 'var(--success)' },
]} />
<Sparkline data={revenue} />`}
>
  <ChartsExample client:visible />
</Preview>

## Progress & steps

<Preview
  code={`import { Progress, CircularProgress, Spinner, Stepper } from '@vespera-ui/react';

<Progress value={64} />
<CircularProgress value={72} />
<Spinner size="lg" />
<Stepper steps={['Account', 'Profile', 'Done']} current={1} />`}
>
  <FeedbackExample client:visible />
</Preview>

## Accordion & tree

<Preview
  code={`import { Accordion, Tree } from '@vespera-ui/react';

<Accordion defaultOpen={[0]} items={[
  { title: 'What is Vespera?', body: 'A deep-space, weightless design system.' },
  { title: 'Is it free?', body: 'Yes — Apache-2.0.' },
]} />
<Tree defaultExpanded={['src']} data={[
  { label: 'src', children: [{ label: 'index.ts', badge: 'ts' }] },
  { label: 'package.json' },
]} />`}
>
  <StructureExample client:visible />
</Preview>

## Command palette

<Preview
  code={`import { CommandPalette, useCmdK } from '@vespera-ui/react';

const [open, setOpen] = useState(false);
useCmdK(setOpen); // ⌘K / Ctrl+K

<CommandPalette open={open} onClose={() => setOpen(false)} groups={[
  { label: 'Actions', items: [
    { label: 'New project', icon: <Icon.plus />, meta: '⌘N' },
    { label: 'Search docs', icon: <Icon.doc />, keywords: 'help' },
  ]},
]} />`}
>
  <CommandExample client:visible />
</Preview>

## Sheet & popover

<Preview
  code={`import { Button, Sheet, Popover } from '@vespera-ui/react';

<Popover trigger={<Button>Popover</Button>}>…anything…</Popover>
<Sheet open={open} onClose={() => setOpen(false)} title="Settings" desc="Manage your workspace.">
  Drawer content
</Sheet>`}
>
  <SheetExample client:visible />
</Preview>

## Date range

<Preview
  code={`import { DateRangePicker } from '@vespera-ui/react';

<DateRangePicker value={range} onChange={setRange} />`}
>
  <DateRangeExample client:visible />
</Preview>

## Breadcrumb & pagination

<Preview
  code={`import { Breadcrumb, Pagination } from '@vespera-ui/react';

<Breadcrumb items={['Home', 'Projects', 'Vespera']} />
<Pagination page={page} pages={8} onPage={setPage} />`}
>
  <NavExample client:visible />
</Preview>

## Banner & empty state

<Preview
  code={`import { Banner, EmptyState, Button, Card } from '@vespera-ui/react';

<Banner tone="accent" action={<Button size="sm">Upgrade</Button>} onDismiss={() => {}}>
  You're on the free plan — upgrade for more.
</Banner>
<Card><EmptyState icon={<Icon.inbox />} title="No messages" action={<Button variant="primary">Compose</Button>} compact /></Card>`}
>
  <MessagingExample client:visible />
</Preview>

## More form controls

<Preview
  code={`import { NumberStepper, InlineEdit, CopyButton, OTPInput } from '@vespera-ui/react';

<NumberStepper value={qty} onChange={setQty} min={0} max={10} />
<InlineEdit value={name} onSave={setName} />
<CopyButton text="npm i @vespera-ui/react" label="Copy install" />
<OTPInput value={otp} onChange={setOtp} length={6} />`}
>
  <MoreFormsExample client:visible />
</Preview>

## Avatars

<Preview
  code={`import { AvatarGroup } from '@vespera-ui/react';

<AvatarGroup max={4} size={40} people={[
  { name: 'Ada Lovelace', hue: 220 },
  { name: 'Alan Turing', hue: 160 },
  // …
]} />`}
>
  <AvatarExample client:visible />
</Preview>

---

That covers most of the library. Browse every component in the
[API reference](/vespera-ui/reference/react/), see the full
[component library showcase](/vespera-ui/examples/library.html), or open the
[interactive playground](/vespera-ui/demo).
