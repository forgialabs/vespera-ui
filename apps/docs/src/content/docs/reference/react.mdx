---
title: React components API
description: Auto-generated props reference for every @vespera-ui/react component, browsable by category.
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import manifest from '@manifest/react.json';
import { slugOf, CATEGORY_BLURB } from '../../../lib/categories.ts';

export const groups = manifest.categories.map((label) => ({
  label,
  slug: slugOf(label),
  blurb: CATEGORY_BLURB[label],
  count: manifest.components.filter((c) => c.category === label).length,
}));

The complete prop reference for `@vespera-ui/react` — **{manifest.components.length} components**,
generated from the package's TypeScript source (run `pnpm manifest` to regenerate). The same data
ships as a machine-readable
[`manifest/react.json`](https://github.com/forgialabs/vespera-ui/blob/main/manifest/react.json)
for AI tools.

Every component renders inside a themed `.vsp-root` and styles itself with
[`@vespera-ui/css`](/vespera-ui/guides/installation/). Icon props accept any node — pass icons
from `@vespera-ui/icons`. Browse by category:

<CardGrid>
  {groups.map((g) => (
    <Card title={`${g.label} · ${g.count}`}>
      {g.blurb}
      <p><a href={`/vespera-ui/reference/${g.slug}/`}>View props →</a></p>
    </Card>
  ))}
</CardGrid>

Prefer live examples? See the [Components](/vespera-ui/components/overview/) section, which mirrors
this same grouping with interactive demos.
