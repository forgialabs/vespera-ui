---
title: React components API
description: Auto-generated props reference for every @vespera-ui/react component.
---

import ReactApi from '../../../components/ReactApi.astro';

The complete prop reference for `@vespera-ui/react`, generated from the package's TypeScript
source (run `pnpm manifest` to regenerate). The same data ships as a machine-readable
[`manifest/react.json`](https://github.com/forgialabs/vespera-ui/blob/main/manifest/react.json)
for AI tools.

Every component renders inside a themed `.vsp-root` and styles itself with
[`@vespera-ui/css`](/vespera-ui/guides/installation/). Icon props accept any node — pass icons
from `@vespera-ui/icons`.

<ReactApi />
