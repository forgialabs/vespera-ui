---
title: Date & time
description: Date picker, date range, and the month/week/agenda event calendar — live examples.
---

import Preview from '../../../components/Preview.astro';

Each of these pops a calendar, so they run in an isolated frame to keep it inside the preview.

## Date picker

<Preview
  code={`import { DatePicker, Field } from '@vespera-ui/react';

<Field label="Date"><DatePicker value={d} onChange={setD} /></Field>`}
  demo="datepicker"
/>

## Date range

<Preview
  code={`import { DateRangePicker } from '@vespera-ui/react';

<DateRangePicker value={range} onChange={setRange} />`}
  demo="daterange"
/>

## Event calendar

Month, week and agenda views in one component. Drag across days in the month grid to add an
event across a range; click an empty week slot to add one at that hour.

<Preview
  code={`import { EventCalendar } from '@vespera-ui/react';

<EventCalendar onChange={(events) => console.log(events)} />`}
  demo="eventcalendar"
/>

Full props: [Date & time API](/vespera-ui/reference/dates/).
