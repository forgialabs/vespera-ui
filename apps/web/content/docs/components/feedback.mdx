---
title: Feedback & status
description: Alerts, progress, banners, and empty states — live examples with the code to copy.
---

## Installation

<Install imports="Alert, Banner, Card, CircularProgress, EmptyState, Progress, Spinner, Stepper" />

## Alerts

<FrameworkPreview
  component="Feedback/Alert"
  story="Tones"
  code={`import { Alert } from '@vespera-ui/react';

<Alert tone="info" title="Heads up">Your trial ends in 3 days.</Alert>
<Alert tone="pos" title="Saved">Your changes are live.</Alert>
<Alert tone="warn" title="Usage high">You're near your API limit.</Alert>`}
/>

## Progress & steps

<FrameworkPreview
  component="Primitives/Nav"
  story="Steps"
  code={`import { Progress, CircularProgress, Spinner, Stepper } from '@vespera-ui/react';

<Progress value={64} />
<CircularProgress value={72} />
<Spinner size="lg" />
<Stepper steps={['Account', 'Profile', 'Done']} current={1} />`}
/>

## Banner & empty state

<FrameworkPreview
  component="Feedback/Messaging"
  story="Banner & empty"
  code={`import { Banner, EmptyState, Button, Card } from '@vespera-ui/react';
import { Icon } from '@vespera-ui/icons';

<Banner tone="accent" action={<Button size="sm">Upgrade</Button>} onDismiss={() => {}}>
  You're on the free plan — upgrade for more.
</Banner>
<Card><EmptyState icon={<Icon.inbox />} title="No messages" action={<Button variant="primary">Compose</Button>} compact /></Card>`}
/>

Full props: [Feedback & status API](/docs/reference/feedback).
