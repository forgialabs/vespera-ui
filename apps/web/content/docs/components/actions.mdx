---
title: Actions
description: Buttons and icon buttons — live examples with the code to copy.
---


## Installation

<Install imports="Button, Tooltip" />

## Buttons

<Preview
  code={`import { Button, Tooltip } from '@vespera-ui/react';
import { Icon } from '@vespera-ui/icons';

<Button variant="primary" leadingIcon={<Icon.check />}>Save</Button>
<Button variant="ghost">Cancel</Button>
<Button variant="outline">Outline</Button>
<Button variant="danger">Delete</Button>
<Button variant="primary" loading>Saving…</Button>
<Tooltip label="Notifications">
  <Button variant="ghost" leadingIcon={<Icon.bell />}>Hover me</Button>
</Tooltip>`}
  demo="button"
/>

### Across frameworks

The same Button rendered live from each framework's own Storybook. Switch tabs to see
the real component per framework; disabled tabs aren't available yet.

<FrameworkPreview
  component="Primitives/Button"
  story="Variants"
  code={`import { Button } from '@vespera-ui/react';

<Button variant="primary">Primary</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="outline">Outline</Button>
<Button variant="subtle">Subtle</Button>
<Button variant="danger">Danger</Button>`}
/>

Full props: [Actions API](/docs/reference/actions).
