/**
 * @vespera-ui/react — React components for the Vespera design system.
 *
 * Phase 2 ports the prototype's `.jsx` primitives here as real, typed ESM modules
 * (dropping the `window` globals), consuming `@vespera-ui/css` for styling and
 * `@vespera-ui/icons` for icons.
 */

export const VERSION = '0.0.0';
