/**
 * @vespera-ui/icons — framework-neutral SVG icon set.
 *
 * Phase 2 ports the prototype's stroke icon set (currently `window.Icon.<name>`)
 * into real, tree-shakeable exports here. Each icon ships as raw SVG path data so
 * any framework wrapper can render it.
 */

export const VERSION = '0.0.0';

/** Placeholder until the icon set is ported in Phase 2. */
export type IconName = never;
