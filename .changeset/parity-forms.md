---
'@vespera-ui/vue': minor
'@vespera-ui/svelte': minor
---

Add form controls to the Vue and Svelte wrappers: `Radio`, `RadioGroup`, `Slider`, and
`NativeSelect`. (Angular gains the same — `vsp-radio`, `vsp-radio-group`, `vsp-slider`,
`vsp-native-select` — published separately.)
