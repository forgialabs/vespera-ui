---
'@vespera-ui/vue': minor
'@vespera-ui/svelte': minor
---

Add `Tooltip` (hover/focus tooltip, top or bottom) to the Vue and Svelte wrappers. Shows on
hover and keyboard focus, positioned against the trigger. (Angular gains `vsp-tooltip` —
published separately.)
