---
'@vespera-ui/vue': minor
'@vespera-ui/svelte': minor
---

Add form-extra components to the Vue and Svelte wrappers: `NumberStepper`, `CopyButton`
(clipboard), and `InlineEdit`. (Angular gains the same — `vsp-number-stepper`, `vsp-copy-button`,
`vsp-inline-edit` — published separately.)
