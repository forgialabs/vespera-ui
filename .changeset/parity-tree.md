---
'@vespera-ui/vue': minor
'@vespera-ui/svelte': minor
---

Add `Tree` (recursive, expand/collapse + selection) to the Vue and Svelte wrappers. (Angular
gains `vsp-tree` — via a recursive `ng-template` — published separately.)
